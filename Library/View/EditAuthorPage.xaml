<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Library.EditAuthorPage">
    <ContentPage.Content>
        <StackLayout>
            <TableView Intent="Data">
                <TableRoot>
                    <TableSection>
                        <ViewCell>
                            <Button x:Name="AddBook" Text="Add book"/>
                        </ViewCell>
                        <EntryCell x:Name="FullName" Label="Full name: " Text="{Binding FullName}"/>
                        <EntryCell x:Name="Birthday" Label="Birthday (year): " Keyboard="Numeric" Text="{Binding Birthday}"/>
                        <TextCell Text="Book "/>
                        <ViewCell>
                            <ListView x:Name="BooksView" HasUnevenRows="True" Grid.Row="1" ItemSelected="OnRemoveBookClicked" Grid.Column="0">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <ViewCell>
                                            <ViewCell.View>
                                                <Label Text="{Binding Title}"/>
                                            </ViewCell.View>
                                        </ViewCell>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </ViewCell>
                    </TableSection>
                </TableRoot>
            </TableView>
            <Button Text="Add/edit" Clicked="OnEditClicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>