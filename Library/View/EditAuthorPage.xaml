<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Library.EditAuthorPage">
    <ContentPage.Content>
        <StackLayout>
            <TableView Intent="Data">
                <TableRoot>
                    <TableSection>
                        <ViewCell>
                            <Button x:Name="AddBook" Text="Add book" Command="{Binding AddBookCommand}"/>
                        </ViewCell>
                        <EntryCell x:Name="FullName" Label="Full name: " Text="{Binding Author.FullName}" IsEnabled="{Binding CouldRename}"/>
                        <ViewCell>
                            <Button Text="{Binding CouldRename}" Command="{Binding RenameAuthorCommand}"/>
                        </ViewCell>
                        <EntryCell x:Name="Birthday" Label="Birthday (year): " Keyboard="Numeric" Text="{Binding Author.Birthday, Mode=TwoWay}"/>
                        <TextCell Text="Book "/>
                        <ViewCell>
                            <ListView x:Name="BooksView" ItemsSource="{Binding Author}" HasUnevenRows="True" ItemSelected="OnRemoveBookClicked">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <ViewCell>
                                            <ViewCell.View>
                                                <Label Text="{Binding Title}"/>
                                            </ViewCell.View>
                                        </ViewCell>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </ViewCell>
                    </TableSection>
                </TableRoot>
            </TableView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>