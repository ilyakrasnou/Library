<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Library.EditAuthorPage">
    <ContentPage.Content>
        <StackLayout>
            <TableView Intent="Data">
                <TableRoot>
                    <TableSection>
                        <!--<ViewCell>
                            <Button x:Name="AddBook" Text="Add book" Command="{Binding AddBookCommand}"/>
                        </ViewCell>-->
                        <EntryCell x:Name="FullName" Label="Full name: " Text="{Binding NewTitle, Mode=TwoWay}" IsEnabled="{Binding CouldRename}"/>
                        <ViewCell>
                            <Button Text="{Binding Rename}" Command="{Binding RenameAuthorCommand}"/>
                        </ViewCell>
                        <ViewCell>
                            <StackLayout>
                                <Image Source="{Binding Author.Photo}"/>
                                <Button Text="Pick photo" Command="{Binding PickPhotoCommand}"/>
                            </StackLayout>
                        </ViewCell>
                        <EntryCell x:Name="Birthday" Label="Birthday (year): " Keyboard="Numeric" Text="{Binding Author.Birthday, Mode=TwoWay}"/>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="Biography"/>
                                <Editor Text="{Binding Author.Biography, Mode=TwoWay}" MaxLength="256"/>
                            </StackLayout>
                        </ViewCell>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal" VerticalOptions="Center">
                                <Label Text="Book " IsVisible="{Binding IsFullAdd}" VerticalOptions="Center" HorizontalOptions="StartAndExpand"/>
                                <Button x:Name="AddBook" Text="Add book" Command="{Binding AddBookCommand}" />
                                <Button x:Name="RemoveAuthor" Text="Remove book" Command="{Binding RemoveBookCommand}" />
                            </StackLayout>
                        </ViewCell>
                        <ViewCell>
                            <ListView x:Name="BooksView" ItemsSource="{Binding Author.Books}" HasUnevenRows="True" ItemSelected="OnRemoveBookClicked">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <ViewCell>
                                            <ViewCell.View>
                                                <Label Text="{Binding Title}"/>
                                            </ViewCell.View>
                                        </ViewCell>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </ViewCell>
                    </TableSection>
                </TableRoot>
            </TableView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>