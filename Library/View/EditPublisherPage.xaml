<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Library.EditPublisherPage">
    <ContentPage.Content>
        <StackLayout>
            <TableView Intent="Data">
                <TableRoot>
                    <TableSection>
                        <!--<ViewCell>
                            <Button x:Name="AddBook" Text="Add book" Command="{Binding AddBookCommand}"/>
                        </ViewCell>-->
                        <EntryCell x:Name="Name" Label="Name: " Text="{Binding Publisher.Name}" IsEnabled="{Binding CouldRename}"/>
                        <ViewCell>
                            <Button Text="{Binding CouldRename}" Command="{Binding RenamePublisherCommand}"/>
                        </ViewCell>
                        <EntryCell x:Name="City" Label="City: " Text="{Binding Publisher.City, Mode=TwoWay}"/>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal" VerticalOptions="Center">
                                <Label Text="Book " VerticalOptions="Center" HorizontalOptions="StartAndExpand"/>
                                <Button x:Name="AddBook" Text="Add book" Command="{Binding AddBookCommand}" />
                                <Button x:Name="RemoveAuthor" Text="Remove book" Command="{Binding RemoveBookCommand}"/>
                            </StackLayout>
                        </ViewCell>
                        <ViewCell>
                            <ListView x:Name="BooksView" ItemsSource="{Binding Publisher.Books}" HasUnevenRows="True" ItemSelected="OnRemoveBookClicked">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <ViewCell>
                                            <ViewCell.View>
                                                <Label Text="{Binding Title}"/>
                                            </ViewCell.View>
                                        </ViewCell>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </ViewCell>
                    </TableSection>
                </TableRoot>
            </TableView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>