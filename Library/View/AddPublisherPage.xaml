<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Library.AddPublisherPage">
    <ContentPage.Content>
        <StackLayout>
            <TableView Intent="Data">
                <TableRoot>
                    <TableSection>
                        <ViewCell>
                            <Button x:Name="AddBook" IsVisible="{Binding IsFullAdd}" Text="Add book" Command="{Binding AddBookCommand}"/>
                        </ViewCell>
                        <EntryCell x:Name="Name" Label="Name: " Text="{Binding Publisher.Name, Mode=TwoWay}"/>
                        <EntryCell x:Name="City" Label="City: " Text="{Binding Publisher.City, Mode=TwoWay}"/>
                        <EntryCell Label="Book "/>
                        <ViewCell>
                            <ListView x:Name="BooksView" IsEnabled="{Binding IsFullAdd}" IsVisible="{Binding IsFullAdd}" ItemsSource="{Binding Publisher}" HasUnevenRows="True" ItemSelected="OnRemoveBookClicked">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <ViewCell>
                                            <ViewCell.View>
                                                <Label Text="{Binding Title}"/>
                                            </ViewCell.View>
                                        </ViewCell>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </ViewCell>
                    </TableSection>
                </TableRoot>
            </TableView>
            <Button Text="Add" Command="{Binding AddPublisherCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>