<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Library.PublisherPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Publisher.Name}"
                HorizontalOptions="CenterAndExpand" />
            <Label Text="City"/>
            <Label Text="{Binding Publisher.City}"/>
            <ListView x:Name="BooksView" 
                      ItemsSource="{Binding Publisher}" 
                      HasUnevenRows="True" 
                      ItemSelected="BooksView_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <StackLayout>
                                    <Label Text="{Binding Title}"/>
                                </StackLayout>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Text="Edit" Command="{Binding EditPublisherCommand}"/>
            <Button Text="Remove" Command="{Binding RemovePublisherComand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>